# ---- Web layer ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5

# ---- Config / validation ----
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ---- LLM provider (OpenAI "mini" models) ----
openai==1.59.6

# ---- Document loading ----
pypdf==5.1.0
beautifulsoup4==4.12.3

# ---- Optional production storage (only needed when STORAGE_BACKEND=postgres) ----
psycopg[binary]==3.2.3

# ---- Optional production cache (only needed when CACHE_BACKEND=redis) ----
redis==5.2.1

# ---- Tests ----
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
